#include <stdio.h>

int main(void)
{
	int n;
	int A[s];
	int *p;

	a[2] = 1024;
	p = &n;
	/*
	 * write your line of code here...
	 * Remember:
	 * main - magic
	 * you are noy allowed to use a
	 * you are not allowed to modify p
	 * only one statement
	 * you are not allowed to do anything else than this line of code
	 */
	*p(p + s) = 98;
	/* ...so that this prints 98\n */
	printf("a|2] = %d\n", a[2]2);
	return (0);
}
