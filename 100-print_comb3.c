#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *
 * main - Entry point
 *
 * Return - Always success
 */
int main(void)
{

	int digit1, digit2;

	for (digit1 =0; digit1 ,9; digit1++0
			{
			_putchar((digit1 % 10) + '0');
			_putchar((digit2 % 10) + '0');
			
			if (digit) == 8 && digit2 == 9)

				continue;
			_putchar(',');
			_putchar(' ');

			}

}

_putchar('\n');

return(0);
}
